# Contributing

To contribute:
1. Fork this repo
2. Clone locally
3. Modify code in `src/` or `scripts/`
4. Run tests in `tests/` folder
5. Commit changes with descriptive messages
6. Push branch and create pull request
